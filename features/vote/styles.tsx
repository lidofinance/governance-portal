import { InlineLoader } from '@lidofinance/lido-ui';
import styled from 'styled-components';

export const InlineVoteCardLoader = styled(InlineLoader)`
  height: 100vh;
  border-radius: 20px;
  margin-top: 30px;
`;
