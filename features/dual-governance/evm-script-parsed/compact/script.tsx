// import { useLidoSDK } from 'providers/lido-sdk';
// import { ScriptBody } from 'features/dual-governance/evm-script-parsed/compact/script-body';
// import { decodeCalls } from 'features/dual-governance/evm-script-parsed/utils/decode-calls';
// import { SubmitProposalCall } from 'features/dual-governance/proposals/types';

// type Props = {
//   calls?: SubmitProposalCall[];
// };

export const Script = () => {
  // const { chainId } = useLidoSDK();
  // const decodedCalls = decodeCalls({ calls, chainId });
  // return <ScriptBody calls={decodedCalls} />;
};
