import { Text } from 'shared/components/text';
import styled from 'styled-components';

export const Wrap = styled(Text).attrs({
  size: 12,
  color: 'secondary',
})`
  margin-top: 2px;
  padding-left: 15px;
`;
